# Повторный аудит и план работ

Дата: 2026-09-01.

Этот документ является текущим источником приоритетов до следующего аудита.
Общий исторический план сохраняется в `docs/work-plan.md`, а подтвержденные
правила старой программы — в `docs/legacy-behavior-reconstruction.md`.

## 1. Итоговая оценка

Buy Modern — рабочий функциональный MVP для демонстрации и приемочного
тестирования. Для реальной ежедневной работы с товаром и деньгами программа пока
не готова: остаются блокеры целостности данных, неподтвержденные правила учета,
незавершенный процесс логиста и неподготовленный перенос начальных данных.

Текущая готовность по направлениям:

| Направление | Оценка | Комментарий |
| --- | --- | --- |
| Справочники | Близко к готовности | Товары, категории, контрагенты и склады работают |
| Документы и склад | MVP+ | Конкурентная защита и версионируемое перепроведение реализованы; финальные правила возвратов остаются |
| Валютные закупки | MVP | Курс фиксируется, но метод себестоимости не определен |
| Оплаты, долги и касса | MVP+ | Запрет отрицательной кассы готов; ручное распределение оплат еще не реализовано |
| Логистика | Частично | Безопасный просмотр есть, складских заданий и подтверждений нет |
| Отчеты и панель | Частично | Основные отчеты есть; панель считает данные из первых 100 записей |
| Аудит | Базово готов | Исполнитель определяется сервером; критичные изменения содержат старое и новое состояние |
| Миграция | Не готова | `BUY.GDB` недоступна, дата среза и контрольные суммы не определены |
| Production | Не готово | CI добавлен; HTTPS/секреты/мониторинг и восстановление резервной копии не подтверждены |

## 2. Что реализовано надежно

- FastAPI, PostgreSQL, Alembic и React/TypeScript разделены по понятным слоям.
- Реализованы роли `admin`, `manager`, `cashier`, `logist`, `viewer` и серверные
  проверки разрешений.
- Справочники, документы, остатки, движения, оплаты, касса, долги, отчеты,
  CSV/XLSX, печать/PDF расходной накладной и Import Lite существуют в одном
  рабочем приложении.
- Проведение документа выполняется транзакционно; при известной ошибке нехватки
  остатка операция откатывается целиком.
- Логист ограничен назначенными складами и отдельной API-схемой без закупочной
  цены, себестоимости, валюты закупки и курса.
- Валюта и курс прихода сохраняются в документе и не зависят от последующего
  изменения справочника курсов.
- Есть backend-тесты и реальные E2E-сценарии с PostgreSQL.

## 3. Блокеры, найденные при аудите

### P0. Целостность данных

1. Устранено в коде: отрицательные количества, цены и суммы блокируются схемами,
   сервисами и ограничениями базы данных.
2. Устранено в коде: касса проверяется по хронологической книге, отрицательный
   остаток запрещен, добавлены PostgreSQL `CHECK`.
3. Устранено в коде: нумерация использует атомарный счетчик и уникальное
   ограничение.
4. Устранено в коде: документ и остатки блокируются при проведении и отмене;
   PostgreSQL-конкурентные тесты ожидают подтверждения CI.
5. Финальные правила отмены, возврата, долга поставщику, переплаты, возврата денег
   и кассовой корректировки еще не подтверждены бизнесом.

### P1. Контроль и полнота процессов

1. Устранено в коде: `AuditLog.actor_user_id` и автор кассовой операции берутся
   из аутентифицированной серверной сессии; клиент не может их подменить.
2. Складское задание логиста отсутствует. Реализован только безопасный просмотр.
3. Панель загружает обычные списки с лимитом 100 и считает показатели в браузере;
   после накопления данных значения будут неполными.
4. Списки документов, оплат, товаров, контрагентов и остатков не имеют полноценной
   серверной пагинации с общим количеством и поиском по всей базе.
5. Перенос данных ограничен справочниками и начальными остатками. Дата среза,
   источники, контрольные суммы и сценарий переключения не заполнены.
6. Реализовано и подтверждено зеленым прогоном: GitHub Actions проверяет backend,
   frontend, PostgreSQL-конкуренцию, mock E2E и real E2E.

### P2. Эксплуатация и удобство

1. Многие ошибки API возвращаются пользователю на английском языке.
2. Документация частично содержит устаревшие статусы и результаты тестов.
3. Нет автоматического ограничения попыток входа, серверного отзыва сессий и
   утвержденной политики паролей.
4. HTTPS, мониторинг, ротация логов, расписание резервных копий и проверка
   восстановления на целевом сервере не настроены.
5. Не согласованы окончательные составы отчетов и печатных форм.

## 4. Подтвержденная модель логиста

- Складское задание — рабочая карточка физического исполнения уже созданного
  менеджером документа; оно не меняет коммерческие условия документа.
- Менеджер проводит документ до работы логиста; остаток меняется при проведении.
- Состояния задания: `Ожидает`, `В работе`, `Выполнено`, `Есть расхождения`,
  `Отменено`.
- Все логисты назначенного склада видят задание, но одновременно обрабатывает его
  только один сотрудник.
- Логист подтверждает фактическое количество по каждой строке; это также может
  сделать менеджер.
- Частичное завершение задания не допускается.
- Недостача, излишек, повреждение или замена не меняют исходный документ
  автоматически, а передаются менеджеру на согласование.
- Серийные номера, сроки годности, фотографии, приоритеты и SLA пока отложены.
- При выдаче фиксируется получатель, окончательную передачу подтверждает менеджер.
- Перемещение учитывает состояние `В пути` и завершается получателем; один логист
  может работать с обоими складами.
- При инвентаризации логист вводит результаты пересчета, корректировку утверждает
  и проводит менеджер; склад при пересчете не блокируется.
- Начало, подтверждение, расхождение, отмена и изменение фактического количества
  обязательно фиксируются в аудите.
- Печатная форма складского задания в первом релизе не требуется.

## 5. Решения по десяти вопросам

| № | Решение | Статус |
| --- | --- | --- |
| 1 | При расхождении разрешено внести изменения в приходную накладную | Подтверждено; реализовать через контролируемое перепроведение |
| 2 | Нумерация без периодического сброса, ручное изменение разрешено | Подтверждено |
| 3 | Проведенные документы разрешено редактировать; отдельных возвратных документов в первом релизе не требуется | Подтверждено |
| 4 | Перемещения будут редкими; при расхождении менеджер выбирает возврат, списание или доприходование | Подтверждено |
| 5 | Себестоимость равна стоимости последней проведенной закупки; предупреждение показывается при наценке ниже 10% | Подтверждено |
| 6 | Продажа создает долг клиента, приход — долг перед поставщиком | Предварительно подтверждено; интерфейс должен разделять «нам должны» и «мы должны» |
| 7 | Менеджер создает, проводит и вручную распределяет оплату по документам | Подтверждено; требуется новая модель распределений и изменение прав |
| 8 | В первом релизе используется одна касса; при корректировке вводится конечный фактический остаток, отрицательная касса запрещена | Подтверждено |
| 9 | Базовый комплект: приходные/расходные накладные, прайс-лист, каталог, долги, расходы/касса, склад и отчеты за период | Подтверждено; точные колонки принимаются на UAT |
| 10 | Целевой переход — конец октября 2026 года, параллельная работа начинается раньше при готовности | Подтверждено без точной даты среза |

Безопасная трактовка редактирования проведенного документа:

1. пользователь открывает режим исправления и вводит причину;
2. система сохраняет неизменяемый снимок старой версии;
3. в одной транзакции отменяет старые складские/долговые последствия;
4. проверяет новую версию, включая остатки, связи и суммы;
5. применяет новые последствия и записывает автора, время, причину и разницу;
6. при любой ошибке весь документ и учет остаются в прежнем состоянии.

Ответы на десять базовых вопросов получены и достаточны для следующего этапа.
Для кассовой корректировки оператор вводит фактический конечный остаток, система
рассчитывает разницу и требует причину. Операция блокируется, если ее результатом
станет отрицательный остаток кассы. Представление долгов проверяется на UAT, а
точная дата среза определяется во время репетиции миграции.

## 6. План работ

Текущий статус выполнения:

| Этап | Статус на 2026-09-01 |
| --- | --- |
| 0. Бизнес-решения | Завершен |
| 1. Защита целостности | Завершен: локальные проверки и полный GitHub Actions CI прошли |
| 2. Учетные правила | Частично: касса, нумерация и версионируемое перепроведение реализованы; распределение оплат и финальные правила возвратов остаются |
| 3. Складские задания | Частично: есть только безопасный read-only экран логиста |
| 4. Интерфейс и отчеты | Частично: базовые экраны/отчеты есть, масштабирование и полная приемка не выполнены |
| 5. Подготовка данных | Частично: Import Lite есть, production-набор и сверка отсутствуют |
| 6. Production и приемка | Частично: Docker-конфигурация есть, сервер и приемка не подготовлены |

### Этап 0. Зафиксировать оставшиеся детали — завершен

1. Ответы на базовые вопросы получены.
2. Решения внесены в доменную документацию и план.
3. Следующий шаг — превратить решения в проверяемые бизнес-сценарии и код.

Критерий: в учете склада и денег нет неотмеченных предположений P0.

### Этап 1. Защитить целостность

1. Запретить отрицательные/недопустимые количества, цены и суммы в Pydantic и
   PostgreSQL; проверить ссылки на товары, склады, документы и пользователей.
2. Сделать конкурентно безопасную нумерацию с уникальным ограничением.
3. Блокировать строки остатков при проведении/отмене и добавить конкурентные
   PostgreSQL-тесты против двойной продажи и потерянного обновления.
4. Передавать текущего пользователя во все мутации, запретить клиенту подменять
   автора и сохранять старые/новые значения критичных полей.
5. Добавить GitHub Actions: backend, typecheck, build, mock E2E и real E2E.

Критерий: некорректный ввод и параллельные операции не нарушают склад, деньги,
нумерацию и аудит.

Статус реализации на 2026-09-01:

1. Добавлены Pydantic-валидации и миграция `0009_integrity_guards` с `CHECK`,
   уникальным номером документа и таблицей непрерывных счетчиков.
2. Номер документа выдается атомарным `UPSERT`; ручной номер резервирует счетчик
   вперед и не может дублировать существующий номер.
3. Проведение и отмена блокируют документ и пары «товар-склад»; добавлены
   PostgreSQL-сценарии параллельной нумерации и двойной продажи.
4. Автор берется только из аутентифицированной сессии, а критичные изменения
   документа, кассы и статуса оплаты сохраняют старое и новое состояние.
5. Касса не может стать отрицательной ни при создании операции, ни при задней
   дате, ни при отмене. Корректировка принимает фактический конечный остаток и
   требует причину.
6. Добавлен GitHub Actions workflow для backend, frontend, mock E2E,
   PostgreSQL-конкурентных тестов и real E2E.
7. Локально пройдено `136` backend-тестов, `2` PostgreSQL-теста корректно
   пропущены без `TEST_POSTGRES_URL`; typecheck, production build и `3` mock E2E
   прошли. В GitHub Actions миграция `0009`, оба PostgreSQL-конкурентных теста и
   real E2E прошли; этап защиты целостности закрыт.

### Этап 2. Завершить учетные правила

1. Реализовано: версионируемое редактирование проведенных документов с атомарным
   перепроведением, обязательной причиной и полной историей.
2. Реализовано: непрерывная уникальная нумерация и контролируемое ручное изменение.
3. Ввести себестоимость по последней проведенной закупке и правило пересмотра
   продажной цены.
4. Привести долги, ручное распределение оплат, авансы и возвраты денег к решениям.
5. Реализовать утвержденные отмены, возвраты и корректировки.
6. Реализовано: одна касса, корректировка до конечного фактического остатка,
   обязательная причина и запрет отрицательного остатка.
7. Покрыть каждый сценарий backend и real E2E-тестами.

Критерий: контрольные остатки, долги и касса совпадают после создания, отмены,
возврата, частичной оплаты и переплаты.

Статус подэтапа 2.1 на 2026-09-01:

1. Миграция `0010_document_revisions` добавляет номер версии проведения,
   явные виды складских движений и неизменяемые снимки версий документа.
2. Исправление проведенного документа передает полную новую версию и причину;
   старая версия сторнируется, новая проверяется и проводится в одной транзакции.
3. Конечный остаток проверяется с учетом старой и новой версии без ложного
   промежуточного минуса; реальный отрицательный результат полностью откатывается.
4. Отмена сторнирует только актуальную версию, а аудит хранит автора, причину,
   старый и новый снимок.
5. В русском интерфейсе добавлены режим исправления, редактирование строк,
   обязательная причина, версия проведения и история версий.
6. Локально пройдено `142` backend-теста (`2` PostgreSQL-теста пропущены до CI),
   frontend typecheck, production build и `3` mock E2E. В real E2E добавлен сценарий
   M08 со сверкой документа, остатка, долга, движений и истории; подтверждение
   миграции `0010` и M08 ожидается в GitHub Actions.

### Этап 3. Реализовать складские задания

1. Добавить задание, строки факта, статусы, исполнителя и журнал переходов.
2. Автоматически создавать задания из проведенных приходов, расходов и
   перемещений; показывать их всем логистам разрешенного склада.
3. Реализовать эксклюзивное взятие в работу, подтверждение строк и передачу
   расхождений менеджеру.
4. Добавить получателя, подтверждение менеджера и состояние товара `В пути`.
5. Добавить пересчет и утверждаемую менеджером корректировку инвентаризации.

Критерий: сценарии приемки, выдачи, перемещения и пересчета проходят с аудитом и
без раскрытия закупочных данных логисту.

### Этап 4. Довести рабочий интерфейс и отчеты

1. Сделать серверные агрегаты панели и корректные периоды/часовой пояс.
2. Добавить серверную пагинацию, поиск и общее количество записей.
3. Утвердить и довести отчеты, экспорт и обязательные печатные формы.
4. Перевести пользовательские ошибки API и добавить состояния загрузки/повтора.
5. Добавить E2E для администратора, кассира, логиста и критичных прав доступа.

Критерий: показатели корректны на объеме больше 100 записей, а ежедневные
операции выполняются без технических сообщений.

### Этап 5. Подготовить данные

1. Утвердить дату среза и шаблоны источников.
2. Подготовить категории, товары, склады, контрагентов, остатки, долги и кассу.
3. Выполнить dry-run и импорт в одноразовой базе.
4. Сверить количество записей и контрольные суммы по каждому складу/контрагенту.
5. Провести репетицию очистки, повторного импорта и переключения.

Критерий: подписанная таблица сверки не содержит необъясненных расхождений.

### Этап 6. Production и приемка

1. Настроить сервер, домен, HTTPS, секреты, ограничение доступа и журналирование.
2. Настроить ежедневные резервные копии вне сервера, мониторинг и уведомления.
3. Восстановить резервную копию в отдельной среде и выполнить smoke-тест.
4. Прогнать ролевые UAT-сценарии менеджера, кассира, логиста и администратора.
5. Выполнить финальный импорт, сверку и контролируемое переключение.

Критерий: CI зеленый, backup восстановлен, UAT подписан, контрольные суммы
совпадают, а откат переключения документирован.

## 7. Целевой календарь

| Период | Результат |
| --- | --- |
| 1–7 сентября | Оставшиеся решения, P0-валидация, аудит автора, CI |
| 8–21 сентября | Безопасное перепроведение, нумерация, себестоимость, долги и оплаты |
| 22 сентября – 5 октября | Складские задания, одна касса, базовые отчеты и ролевые E2E |
| 6–15 октября | Подготовка данных, первый тестовый импорт и сверка |
| 16–24 октября | Production-среда, backup/restore, UAT и исправления |
| 25–31 октября | Параллельная работа, финальная сверка и переход |

Календарь реалистичен только при непрерывной реализации release-блокеров и
доступности исходных таблиц с остатками, долгами и кассой не позднее начала
октября.

## 8. Проверки текущего аудита

- рабочее дерево до документирования аудита было чистым, `main == origin/main`;
- backend на SQLite: `123 passed`;
- frontend TypeScript: passed;
- frontend production build: passed (`1614` modules);
- mock Playwright: `3 passed`;
- `npm audit --offline`: `0 vulnerabilities` по локальному lock-файлу;
- отрицательные количество, цена и кассовая сумма подтвержденно принимаются
  текущими API-схемами;
- Docker Desktop не запущен, поэтому PostgreSQL-набор `7 real E2E` и миграция на
  чистой базе 2026-09-01 повторно не проверялись. Последний сохраненный успешный
  результат этих сценариев датирован 2026-07-24.
